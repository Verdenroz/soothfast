# Code generated by `cargo soothfast sdk gen`. DO NOT EDIT.
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "acme-items"
version = "1.2.3"
description = "Acme items client"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[project.urls]
Repository = "https://github.com/acme/items"

[tool.hatch.build]
ignore-vcs = true

[tool.hatch.build.targets.wheel]
packages = ["src/acme_items"]
