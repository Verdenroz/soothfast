# stats

Robust summary statistics and the FNV-1a fingerprint soothfast gates on

C bindings for the `soothfast_demo` crate, generated by [soothfast](https://github.com/Verdenroz/soothfast). Do not edit by hand: run `cargo soothfast bind gen` instead.

## Build

```bash
cargo build --release
```

That writes `target/release/libstats.so` (`.dylib` on macOS, `.dll` on Windows) and `target/release/libstats.a`. The header is `stats.h`.

## Use

```c
#include "stats.h"
```

## Ownership

Anything this library returns by pointer is yours to release:

- a handle, with its `*_free`
- a sequence, with its `*_array_free`
- a string, with `stats_string_free`

A call that can fail takes a trailing `char **error`. On failure it writes a message there and returns a zero value you must not read; on success it writes `NULL`. Passing `NULL` for `error` discards the message. Every message is a string you release the same way.

## Types

### `stats_metric`

Which statistic a comparison reads.


### `stats_summary`

A robust summary of one sample set.

- `stats_summary_new`
- `stats_summary_deviations`
- `stats_summary_deviations_all`
- `stats_summary_deviations_into`
- `stats_summary_get`
- `stats_summary_label`
- `stats_summary_rescale`
- `stats_summary_parse`

## Functions

- `stats_fingerprint`


## C++

This package also ships a header-only C++20 wrapper (`stats.hpp`) generated over the same header and library, in namespace `soothfast::stats`. Build the library first, then `#include "stats.hpp"` and link against it the same way a C consumer would.
