# Code generated by `cargo soothfast bind gen`. DO NOT EDIT.

[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "soothfast-stats"
version = "0.1.15"
requires-python = ">=3.9"
description = "Robust summary statistics and the FNV-1a fingerprint soothfast gates on"

[tool.maturin]
module-name = "soothfast_stats"
features = ["pyo3/extension-module"]
