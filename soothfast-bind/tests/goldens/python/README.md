# acme-core

Python bindings for the `acme` crate, generated by [soothfast](https://github.com/Verdenroz/soothfast). Do not edit by hand: run `cargo soothfast bind gen` instead.

## Install

```bash
pip install acme-core
```

The wheel ships `py.typed` and a stub, so editors and type checkers see the surface.

## Use

```python
import acme_core
```

## Classes

### `Counter`

- `absorb`
- `at`
- `bump`
- `bump_all`
- `refresh`
- `scale`

### `Level`


### `Mode`


## Functions

- `counters`
- `describe`
- `describe_owned`
- `digest`
- `fail`
- `find_counter`
- `flags`
- `greet`
- `index_all`
- `invert_bits`
- `is_high`
- `levels`
- `maybe_ratio`
- `mutate_counter`
- `normalize`
- `peak_level`
- `peek`
- `sample_ids`
- `scale_into`
- `scale_optional`
- `set_level`
- `split`
- `stamp`
- `sum_optional`
- `trim`
- `version`

## Not bound

- acme::Counter::consume: takes `self` by value, which would empty the handle the binding holds; take `&self` and return a new value instead
- acme::with_time: foreign type `chrono::DateTime` has no mapping; map it under [bind.types] in soothfast.toml (`"chrono::DateTime" = "str"` crosses it as a string through Display and FromStr)
- acme::merge: crosses the exported type `Counter` by value, which would copy it; derive `Clone` on it (Python then reads a field of it as a fresh handle), take it by reference, or add a method returning what the caller needs
