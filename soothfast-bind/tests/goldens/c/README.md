# acme-core

C bindings for the `acme` crate, generated by [soothfast](https://github.com/Verdenroz/soothfast). Do not edit by hand: run `cargo soothfast bind gen` instead.

## Build

```bash
cargo build --release
```

That writes `target/release/libacme_core.so` (`.dylib` on macOS, `.dll` on Windows) and `target/release/libacme_core.a`. The header is `acme_core.h`.

## Use

```c
#include "acme_core.h"
```

## Ownership

Anything this library returns by pointer is yours to release:

- a handle, with its `*_free`
- a sequence, with its `*_array_free`
- a string, with `acme_core_string_free`

A call that can fail takes a trailing `char **error`. On failure it writes a message there and returns a zero value you must not read; on success it writes `NULL`. Passing `NULL` for `error` discards the message. Every message is a string you release the same way.

## Types

### `acme_core_counter`

- `acme_core_counter_new`
- `acme_core_counter_absorb`
- `acme_core_counter_at`
- `acme_core_counter_bump`
- `acme_core_counter_bump_all`
- `acme_core_counter_scale`

### `acme_core_level`


### `acme_core_mode`


## Functions

- `acme_core_describe`
- `acme_core_describe_owned`
- `acme_core_digest`
- `acme_core_fail`
- `acme_core_find_counter`
- `acme_core_flags`
- `acme_core_greet`
- `acme_core_invert_bits`
- `acme_core_is_high`
- `acme_core_mutate_counter`
- `acme_core_normalize`
- `acme_core_peak_level`
- `acme_core_sample_ids`
- `acme_core_scale_into`
- `acme_core_split`
- `acme_core_stamp`
- `acme_core_version`

## Not bound

- acme::Counter::consume: takes `self` by value, which would empty the handle the binding holds; take `&self` and return a new value instead
- acme::with_time: foreign type `chrono::DateTime` has no mapping; map it under [bind.types] in soothfast.toml (`"chrono::DateTime" = "str"` crosses it as a string through Display and FromStr)
- acme::Counter::refresh: `async fn` cannot cross into c: C has nothing to await with; expose a blocking wrapper instead
- acme::counters: `Vec<Counter>` cannot cross into c: a sequence of `Counter` has no C spelling that owns its elements; a sequence of one primitive crosses as a pointer and a length
- acme::index_all: `HashMap<String, u32>` cannot cross into c: C has no map type; return a sequence of pairs, or an exported type with accessors
- acme::levels: `Vec<Level>` cannot cross into c: a sequence of `Level` has no C spelling that owns its elements; a sequence of one primitive crosses as a pointer and a length
- acme::maybe_ratio: `Option<f64>` cannot cross into c: `Option<f64>` has no C spelling; only an optional exported type or string does, as a pointer that may be null
- acme::merge: crosses the exported type `Counter` by value, which would copy it; derive `Clone` on it (Python then reads a field of it as a fresh handle), take it by reference, or add a method returning what the caller needs
- acme::peek: `Option<Counter>` cannot cross into c: an optional exported type is taken only as a return; take it by reference instead
- acme::scale_optional: `Option<f64>` cannot cross into c: `Option<f64>` has no C spelling; only an optional exported type or string does, as a pointer that may be null
- acme::set_level: `Option<Level>` cannot cross into c: an optional exported type is taken only as a return; take it by reference instead
- acme::sum_optional: `Option<Vec<f64>>` cannot cross into c: `Option<Vec<f64>>` has no C spelling; only an optional exported type or string does, as a pointer that may be null
- acme::trim: `Option<Vec<f64>>` cannot cross into c: `Option<Vec<f64>>` has no C spelling; only an optional exported type or string does, as a pointer that may be null
