[package]
name = "soothfast-registry"
description = "Distributed registry of measured and documented items"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
linkme.workspace = true

# Path-only (no version): stripped on publish, so no dev-dep cycle on crates.io.
[dev-dependencies]
soothfast = { path = "../soothfast", features = ["runner"] }

[[bench]]
name = "soothfast"
harness = false
